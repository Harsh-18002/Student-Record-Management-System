# Student-Record-Management-System
Python app (SRMS) to automate manual student grading and record keeping. It uses basic procedural structures: sequential if/elif for grade calculation and a manual while loop for search. 
